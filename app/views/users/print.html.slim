#customer
  table#meta
    tr
      td.meta-head Daftar Pengguna
      td
        - if @user.present?
          textarea #{@user.name}
        - else
          textarea SEMUA
    tr
      td.meta-head Kriteria
      td
        | -


table#items
  tr colspan="5"

  tr
    th Nama
    th Email
    th Toko

  - @users.each do |user|
    tr.item-row
      td #{user.name}
      td #{user.email}
      td #{user.store.name}